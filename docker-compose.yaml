services:
  service-email:
    image: service-email
    ports:
      - "3000:3000"
    build:
      context: .
      dockerfile: dockerfile.prd
    networks:
      - local-net
    restart: always
networks:
  local-net:
    driver: bridge